taskName = "Rectangle area";

function Main(bufferElement) {

    var width = 3;
    var height = 4;
    var result = width * height;
    WriteLine('width = ' + width + ', height = ' + height + ', Area = ' + result);

    var width = 2.5;
    var height = 3;
    var result = width * height;
    WriteLine('width = ' + width + ', height = ' + height + ', Area = ' + result);

    var width = 5;
    var height = 5;
    var result = width * height;
    WriteLine('width = ' + width + ', height = ' + height + ', Area = ' + result);
}